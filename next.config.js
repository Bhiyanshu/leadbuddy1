/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['your-domain.com'], // Replace with your actual domain(s)
  },
};

module.exports = nextConfig;
